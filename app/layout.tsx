import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "BookCryto - Blockchain-Powered Literature Platform",
  description:
    "Read authentic novels, earn tokens, and join a vibrant literary community powered by blockchain technology.",
  keywords: "blockchain, literature, novels, tokens, reading, community, e-commerce",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
